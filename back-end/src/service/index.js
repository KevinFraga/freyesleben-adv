const user = require('./userService');
const file = require('./fileService');
const post = require('./postService');
const process = require('./processService');

module.exports = {
  user,
  file,
  post,
  process,
};
